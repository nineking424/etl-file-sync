[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (requires FTP servers)
    e2e: End-to-end tests (requires full stack: FTP + Kafka container)
